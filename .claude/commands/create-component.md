Create a new React component following the frontend-dashboard skill patterns:

1. Create component in appropriate `frontend/src/components/` subdirectory
2. Use TypeScript with proper interface definitions
3. Implement with React functional component and hooks
4. Style with Tailwind CSS utility classes
5. Add to parent component or page

Follow naming conventions and keep components focused and reusable.
